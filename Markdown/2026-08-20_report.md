# 작업 보고 — 2026-08-20

> **작성일** 2026-08-20
> **작성** Claude Opus 5 (Claude Code)
> **측정 데이터** `python/test_20260820/` (커밋 `0b81471`의 `python/Measure_20260820.zip`)
> **주의** AI가 생성한 문서입니다. 수치·절차는 사용 전 실제 코드/측정과 대조하십시오.

---

## 0. 요약

1. `Markdown/2026-08-19_mode0_plan.md`의 3단계(Phase 1/2/3)를 **전부 수행**했다.
2. **교정 효과 확인** — 이득 오차 0.76 % → 0.11 %, INL p-p 113.0 → 78.2 ps.
3. **single-shot precision이 양자화 한계에 도달**해 있음을 5개 지점에서 0.01 ps 이내로 확인.
4. **★ 그동안의 "빌드 군집" 미스터리가 해소됐다 — 원인은 Zybo 보드가 2대라서였다.**
5. `RTL/dna_reader.v` 신규 — 칩 고유 ID를 ILA에 찍어 보드를 데이터에 영구 기록.
6. CDC 결손 수정(사용자 작업, 커밋 `0b81471`).

---

## 1. ★ 빌드 군집의 정체 — 보드가 2대였다

### 1-1. 확정된 대응

이전 세션에서 "빌드마다 지연선이 두 상태 중 하나로 갈린다"고 했던 현상의 원인이다.

| | 보드 | 유효탭 | LSB | f_RO (CO plain 빌드) | 해당 측정 |
|---|---|---|---|---|---|
| **군집 A** | **집** | 299 | 16.722 ps | 62.850 ~ 62.890 MHz | 8/04 밤, 8/09 일, 8/12, 8/13 |
| **군집 B** | **회사** | 284 | 17.606 ps | 63.847 ~ 63.884 MHz | 8/06 온도셋, **8/20 오늘** |

**용어**: *군집* = 지연선이 사실상 같은 캡처들의 묶음. 같은 군집끼리 탭 폭 상관
**r = 0.999**, 다른 군집끼리 **r = 0.54**. 중간값이 없다.

`w[i] = h[i]/H × 5000 ps` (탭 폭), `H` = 총 히트. **유효 탭** = `h[i] > 0` 인 탭 수.

### 1-2. 근거 셋

**(a) 온도 챔버가 회사에만 있다.** 따라서 모든 온도 데이터는 회사 보드다. 레포에 커밋된
온도 zip 3개를 풀어 확인:

| zip | 커밋 | f_RO |
|---|---|---|
| `python/temp.zip` | 7/28 | 63.598 ~ 63.675 MHz |
| `python/co_temp.zip` | 8/06 | 63.862 ~ 63.864 MHz |
| `python/o_rebuild.zip` | 8/10 | 64.674 ~ 64.733 MHz |

`co_temp`(8/06)가 군집 B이고, 오늘(8/20)도 군집 B다. **같은 회사 보드다.**

**(b) 회사에서는 Claude를 쓸 수 없다.** 8/12·8/13 측정은 그 세션에서 Claude가 새로 만든
파일로 빌드했다 — `tdc_fmcw_core_mix.v`, `tdc_fmcw_core_mix_thermo.v`,
`tdc_fmcw_core_co_thermo.v` + top 임시 수정. **이 파일들은 그때 커밋된 적이 없어
(2026-08-19에야 커밋) 회사 장비에는 존재하지도 않았다.** 따라서 집 보드다.
그리고 그 캡처들이 전부 군집 A다.

**(c) 링 오실레이터 주파수가 칩 지문 역할을 한다.** `f_RO = ro_meas_cnt × 400 Hz`
(10 ms 게이트, `ro_divider_cnt[1]` = RO의 1/4 주기).
같은 종류 빌드(CO plain)에서:
```
집  : 62.890 / 62.878 / 62.889 (8/04)   62.850 / 62.854 (8/09)   62.852 (8/13)
회사 : 63.864 / 63.862 (8/06)            63.884 / 63.868 (8/20)
       ↑ 폭 0.04 MHz 안에 모이고, 두 집단이 1.0 MHz 떨어져 겹치지 않는다
```
빌드가 달라도 f_RO가 0.04 MHz 안에 들어오는데 군집을 넘으면 1 MHz 뛴다.
**다만 RO에 LOC 제약이 없어 빌드가 크게 바뀌면 f_RO도 바뀐다**(같은 집 보드에서
62.85 / 65.85 / 69.77). 따라서 **같은 종류 빌드끼리만** 지문으로 쓸 수 있다.

### 1-3. 왜 칩이 바뀌면 지연선이 바뀌는가

`tdc.xdc`가 CARRY4 80단과 샘플링 FF 320개를 **같은 슬라이스에 LOC/BEL로 못박아** 놓았다.
그래서 탭 폭은 **그 슬라이스의 실리콘이 결정**한다.

> **같은 칩 → 빌드가 달라도 탭 폭 동일 (r = 0.999)**
> **다른 칩 → 공정 편차로 탭 폭 상이 (r = 0.54)**

회사 보드가 집 보드보다 **5.3 % 느리다** (LSB 17.606 vs 16.722). 공정 코너 차이로 흔한 폭이다.

### 1-4. 철회 — "P&R이 두 해로 수렴한다"

2026-08-19 보고서 §1-3에서 세운 가설이다. **철회한다.** 보드 차이가 훨씬 단순하고
모든 데이터에 맞는다. 같은 보고서의 "`co_temp/`는 현재 빌드와 다른 빌드"라는 서술도
**"다른 보드"로 정정**한다(그 문서에 정정 이력을 남겼다).

### 1-5. 실무적 결론

- **오늘(8/20) 결과와 온도셋(8/06)은 같은 보드다. 함께 쓸 수 있다.**
- **8/12·8/13 데이터(집 보드)는 오늘 결과와 섞으면 안 된다.**
- 8/10 커밋 `1c63334`의 "재합성만으로 −49 %" 결론은 7/28·8/10 둘 다 회사 보드(온도 챔버)이므로
  **보드 교란이 없다. 유효하다.** (한때 의심했으나 근거 없었다.)
- 8/09의 O vs CO 빌드 통제 비교는 같은 날 같은 보드(집)라 **유효하다.**

### 1-6. 남는 어긋남

파일 mtime이 **8/12 수 16:25, 8/13 목 10:48~13:58**로 근무시간대인데 출근하신 날이다.
mtime이 측정 시각이 아닐 가능성이 있다. **위 결론은 바뀌지 않는다** — 근거 (b)가 더 강하다.

---

## 2. Phase 1 — 코드밀도 (Mode 1, Ring Osc)

### 2-1. 결과 (측정)

| 캡처 | 유효탭 | 총 히트 | LSB | DNL rms | DNL max | DNL min | INL p-p |
|---|---|---|---|---|---|---|---|
| `ro_cal` | 284 | 330,354,107 | 17.606 ps | 0.705 | 2.604 | −0.999 | 105.1 ps |
| `ro_val1` | 284 | 184,849,481 | 17.606 | 0.704 | 2.567 | −1.000 | 106.3 |
| `ro_val2` | 284 | 208,516,259 | 17.606 | 0.704 | 2.564 | −0.999 | 106.6 |

세 캡처 서로 **r = 0.9998 ~ 0.9999**. 측정 재현성 문제없음.

**`%4` 그룹 평균 폭** (`%4` = 탭 인덱스 mod 4 = CARRY4 안 위치):

| | %4==0 | %4==1 | %4==2 | %4==3 | 그룹 표준편차 |
|---|---|---|---|---|---|
| 8/20 `ro_cal` | 16.10 | 30.67 | 4.84 | 18.82 | 9.19 ps |
| 8/06 `co_temp` T40 | 16.04 | 30.65 | 4.85 | 18.88 | 9.17 ps |

같은 보드이므로 사실상 동일하다 (`ro_cal` ↔ 8/06 T40: **r = 0.9992**, 평균&#124;차&#124; 0.34 ps, 최대 2.43 ps).

### 2-2. 양자화 한계 (유도)

`p[i] = h[i]/H` 로 두고:

| 양 | 값 | 계산식 |
|---|---|---|
| LSB (산술평균 탭 폭) | 17.606 ps | 5000 / 284 |
| **σ 기대 (양자화 한계)** | **8.20 ps** | √( Σ w[i]²·p[i] / 12 ) |
| 탭이 균일했다면 | 5.08 ps | LSB / √12 |

---

## 3. Phase 2/3 — 전달함수 (진짜 시간 기준) ★ 핵심

참 시간 = `loop_cnt × PHASE_STEP`, `PHASE_STEP = 1000/56 ≈ 17.857 ps`.
측정 fine = `(5000 − timestamp mod 5000) mod 5000` (coarse 성분 소거).

### 3-1. 개별 캡처 (측정)

| 캡처 | 점수 | 기울기 | INL p-p | INL rms | DNL max | DNL rms | 다이온도 |
|---|---|---|---|---|---|---|---|
| `before_capture_1` | 280 | 0.9926 | 118.3 ps | 20.9 ps | 3.03 | 1.10 | 38.7 °C |
| `before_capture_2` | 280 | 0.9922 | 136.9 | 21.0 | 3.03 | 1.09 | 38.6 |
| `before_capture_3` | 280 | 0.9924 | 119.6 | 21.3 | 3.98 | 1.14 | 38.7 |
| `after_capture_1` | 280 | 1.0010 | 96.6 | 18.8 | 3.31 | 0.99 | 39.2 |
| `after_capture_2` | 280 | 1.0012 | 95.8 | 17.8 | 4.04 | 1.08 | 39.2 |
| `after_capture_3` | 280 | 1.0011 | 91.1 | 18.7 | 3.31 | 1.07 | 39.2 |

### 3-2. 3회 평균 후 (측정 + 유도)

| | 기울기 | 이득 오차 | INL p-p | INL rms |
|---|---|---|---|---|
| **BEFORE** (선형 COE) | 0.9924 | **0.76 %** | **113.0 ps** | 18.7 ps |
| **AFTER** (코드밀도 LUT) | **1.0011** | **0.11 %** | **78.2 ps** | 15.9 ps |
| 개선 | — | **7배** | **−31 %** | −15 % |

**기울기가 이론과 맞는지 검증(유도):**
- BEFORE 예측 = 279탭 × 17.857 ps / 5000 ps = **0.9964** (실측 0.9924)
- AFTER 예측 = **1.0** (LUT가 누적 시간축이므로) (실측 1.0011)

**이득 보정이 가장 확실한 성과다.**

### 3-3. 측정 잡음 분리

반복 3회 캡처에서 같은 스텝을 3번 잰 값의 표준편차 = **점당 측정 잡음 σₙ**:

```
BEFORE  σₙ = 7.79 ps      AFTER  σₙ = 7.89 ps
```

§2-2에서 히스토그램으로 유도한 양자화 한계 **8.20 ps**와 일치한다. **독립적인 교차 확인이다.**

잡음 제거 후 INL rms: BEFORE **18.2 ps**, AFTER **15.3 ps**.

### 3-4. BEFORE에 로드된 COE — 역추출

측정된 fine 값이 곧 ROM 엔트리이므로 그대로 복원된다:
```
BEFORE 관측 : 0 18 36 54 71 89 125 143 161 179 196 214 232 250 …
dummy_rom   : 0 18 36 54 71 89 107 125 143 161 179 196 214 232 250 …
```
**`dummy_rom.coe`(17.857 ps/탭)가 로드돼 있었다.** 계획서 §1-2에서 오버플로 위험으로
쓰지 말라고 한 파일이다.

**결과적으로 오버플로는 일어나지 않았다.** 관측된 최대 fine이 **4,982 ps**(idx 279)로
5,000 미만이다. 다만 유효탭이 284개라 idx 283이 잡혔다면 5,054 ps로 넘쳤을 것이다.

AFTER의 fine 값은 222개, 범위 14 ~ 4,975 ps, 인접 차이 5 ~ 49 ps로 **불균일** —
코드밀도 LUT가 제대로 적재됐다는 뜻이다.

### 3-5. 교정 후에도 INL p-p 78 ps가 남는 이유

**버그가 아니라 원리적 한계다.** 코드밀도 LUT는 각 히트를 bin 중심에 놓는 것이 최선이므로,
넓은 bin이 만드는 오차는 못 없앤다. 이 빌드의 DNL max 2.604 → 최대 탭 폭 ≈ 63 ps이고,
그 탭 하나에서만 ±32 ps 오차가 난다.

> **"정적 LUT의 잔차는 bin 폭 분포가 결정한다"** — 정량적 결론으로 논문에 쓸 수 있다.

---

## 4. Phase 3 — 지터: 순수 양자화

`current_loop_cnt`를 N에 고정하고 8,192 사이클을 통째로 캡처. hit이 10클럭(50 ns)마다
결정론적으로 나오므로 유효 히트 **820개**(= 8192/10)를 추출했다.

각 위상에서 히트가 대부분 **인접한 두 탭**에만 떨어진다. 두 탭 간격 Δ, 한쪽 비율 p일 때
2준위 양자화의 표준편차는 `σ = Δ·√(p(1−p))`:

| 파일 | loop_cnt | Δ [ps] | p | **예측 σ** | **실측 σ** | 차이 | 상위2탭 점유 |
|---|---|---|---|---|---|---|---|
| `jitter_50` | 50 | 35 | 0.706 | 15.94 | **15.95** | +0.01 | 73.9 % |
| `jitter_100` | 100 | 10 | 0.560 | 4.96 | **4.97** | +0.00 | 94.1 % |
| `jitter_150` | 150 | 29 | 0.683 | 13.50 | **13.51** | +0.01 | 99.5 % |
| `jitter_200` | 200 | 25 | 0.858 | 8.73 | **8.74** | +0.01 | 81.5 % |
| `jitter_250` | 250 | 32 | 0.517 | 15.99 | **16.00** | +0.01 | 90.6 % |

(전체 탭을 포함한 σ는 7.29 ~ 19.65 ps. 위 표는 상위 2탭만.)

> **다섯 지점 전부 0.01 ps 이내로 일치한다. 아날로그 지터의 기여가 측정에 전혀 나타나지 않는다.**
> **이 TDC의 single-shot precision은 양자화 한계에 도달해 있다.**

σ는 그 지점의 **탭 간격에 좌우될 뿐** TDC의 잡음이 아니다. §3-3의 σₙ = 7.8 ps와
§2-2의 8.20 ps가 일치하는 것이 이를 뒷받침한다.

**논문에 붙일 단서**: hit과 샘플링 클럭이 동일 MMCM에서 유래하므로 두 출력에 공통인
지터는 상쇄되어 이 값에 포함되지 않는다. 이 σ는 TDC 자체의 분해능이며 외부 신호원을
포함한 시스템 지터가 아니다.

---

## 5. 개선이 필요한 것

### 5-1. 전달함수가 스텝당 1샘플뿐이다

`capture_trigger`가 스텝당 1회만 발동하므로 각 점의 오차가 **σₙ ≈ 7.8 ps**다.
관측 INL rms 18.7 ps 중 상당 부분이 이 측정 잡음이다.

**해결안**: `capture_arm`을 카운터로 바꿔 스텝당 **K개** 히트를 통과시킨다.
K=16이면 281 × 16 = 4,496 샘플로 depth 8192에 들어가고, 잡음이 **4배** 줄어 ~2 ps가 된다.
RTL 몇 줄. **미결정.**

### 5-2. Mode 1 LUT를 Mode 0에 적용한 것의 검증이 빠졌다

Mode 1은 `clk_200_fixed`, Mode 0은 `clk_200_shifted`로 클럭이 다르다. 계획서 §6-3에서
지적한 교차확인 — **Mode 0 히스토그램**을 아직 못 떴다. `after` 빌드에서 히스토그램을
한 번 뜨면 `ro_cal`과 대조해 LUT의 적절성을 확정할 수 있다. **미수행.**

---

## 6. RTL 변경

### 6-1. CDC 결손 수정 (사용자 작업, 커밋 `0b81471`)

`current_loop_cnt`(9비트)를 이종 클럭(`clk_200_fixed` → `tdc_clk`)에서 직접 비교하던 것이
버스 스큐·메타스테빌리티로 스텝 결손/중복 트리거를 유발했다.

- `phase_shifter.v` — 1비트 `loop_updated_toggle` 출력 추가
- `tdc_test_top.v` — `ASYNC_REG` 2단 동기화 → 에지 펄스 → `current_loop_cnt_stable` 래치
- ILA `probe2`를 `current_loop_cnt_stable`로 교체

### 6-2. Device DNA 보드 식별자 (신규, 2026-08-20)

**`RTL/dna_reader.v`** (신규) — `DNA_PORT`에서 57비트 칩 고유 ID를 리셋 직후 1회 읽어 고정.
`READ` 1펄스 → `SHIFT` 57회. `CLK_DIV=16`으로 12.5 MHz 저속 클럭 사용(보수적).

**`RTL/tdc_test_top.v`** — 인스턴스 + ILA 배선:
```verilog
.probe9 ({device_dna_valid, device_dna[30:0]})   // [31:0] 보드 식별자
```

> **★ ILA IP 재구성 필요: probe 수 9 → 10, probe9 폭 32비트. probe0~8은 그대로.**

프로브를 **추가만** 하므로 기존 신호가 죽지 않는다. 8/13에 `dbg_thermo`를 넣었을 때
`final_timestamp_ps`가 죽어 DSP·교정ROM·파이프라인이 통째로 사라졌던 것과 다르다.
탭 폭이 흔들릴 우려도 근거가 약하다 — 오늘 CDC 수정(top + phase_shifter 변경)에도
탭 폭이 8/06과 **r = 0.9992**로 유지됐다.

**읽는 법**: 최상위 1비트가 `dna_valid`. 0이면 그 캡처의 식별값을 믿지 말 것.
비트 순서는 Xilinx 공식 DNA 표기와 다를 수 있다(**확인 안 함**). 보드 구분이 목적이라 무관하다.

**빌드 시 `CLOCK_DEDICATED_ROUTE` 경고가 나면** `tdc.xdc`에 추가:
```tcl
set_false_path -to [get_pins -hier -filter {NAME =~ *u_dna*DNA_PORT*/CLK}]
```

---

## 7. 파일

### 커밋 대상 (이 커밋)

| 파일 | 상태 |
|---|---|
| `RTL/dna_reader.v` | 신규 151줄 |
| `RTL/tdc_test_top.v` | 수정 — DNA 인스턴스 + `probe9` |
| `Markdown/2026-08-20_report.md` | 신규 (이 문서) |
| `Markdown/2026-08-19_report.md` | 수정 — §1-3 "다른 빌드" → "다른 보드" 정정 |

### 측정 데이터

`python/test_20260820/` (18파일). 커밋 `0b81471`의 `python/Measure_20260820.zip`에 포함.

| 파일 | 내용 |
|---|---|
| `ro_cal.csv`, `ro_val1.csv`, `ro_val2.csv` | Phase 1 히스토그램 |
| `tap_histogram_20260820_*.csv` ×3 | `Histogram.py` 출력 |
| `before_capture_1/2/3.csv` | Phase 2 전달함수 (선형 COE) |
| `after_capture_1/2/3.csv` | Phase 3 전달함수 (코드밀도 LUT) |
| `jitter_50/100/150/200/250.csv` | Phase 3 지터 |

`python/calc_jitter.py` 신규(사용자 작성).

---

## 8. 미결정 사항

1. **`capture_arm` 카운터화** (§5-1) — 전달함수 측정 잡음 4배 감소.
2. **Mode 0 히스토그램 교차확인** (§5-2) — Mode 1 LUT의 Mode 0 적용 타당성.
3. **링 오실레이터 LOC 고정** — 빌드마다 f_RO가 크게 변한다(62.85 / 65.85 / 69.77 MHz,
   같은 보드). 빌드 간 비교의 전제가 흔들린다. 이월.
4. **`tdc_fmcw_core_co.v` 헤더의 thermometer 도착 순서 주석** — "그 빌드·그 보드 한정"
   단서를 붙일지. 이월.
5. **`Markdown/fig/2026-08-13_mod4_thermo.png` 파일명** — 생성일은 8/19. 이월.
6. **미보정 문서** — `Markdown/2026-08-10_report.md` §1/§4 정규화 오류,
   `Markdown/2026-07-29_report.md` §2-0/§2-2 metric A 수치. 이월.

---

## 9. 다음 측정 시 주의

- **보드를 기록할 것.** 다음 빌드부터 `probe9`에 칩 ID가 박힌다. 두 보드에서 한 번씩 떠서
  어느 값이 회사이고 어느 값이 집인지 이 문서에 기록해 둘 것.
- **`dummy_rom.coe` 대신 유효탭 수에 맞는 선형 COE를 쓸 것** (§3-4). 이 보드는 284탭이므로
  5000/284 = 17.606 ps/탭이 적절하다.
- **온도 실험은 회사 보드에서만 가능하다**(챔버). 집 보드 데이터와 섞지 말 것.
