# -*- coding: utf-8 -*-
"""
TDC_Full_Project_Report.md 파일을 자동 생성하는 파이썬 스크립트
(초기 LUT 보정부터 64비트 절대시간 연산 및 최종 시각화까지 전체 진행 사항 통합본)
"""

report_content = r"""# 64-bit TDC Calibration 및 절대 시간 측정 시스템 종합 분석 보고서

## 1. 개요 (Overview)
본 보고서는 FPGA 내부의 **TDC(Time-to-Digital Converter)** 딜레이 라인 측정 데이터를 기반으로 하드웨어 캘리브레이션 LUT를 생성하고, 이를 활용하여 **오차율 0%에 수렴하는 64비트 절대 시간(Absolute Time) 측정기**를 설계 및 검증한 전체 프로젝트의 진행 사항을 기술합니다.

특히, 랩어라운드(Wrap-around) 및 메타스테빌리티로 인한 하드웨어의 물리적 한계를 소프트웨어 알고리즘으로 극복하고, 고속(200MHz) 타이밍 클로저를 만족하는 5-Stage 파이프라인을 설계하여 **최종적으로 17.77 ps의 극한의 정밀도**를 달성한 과정을 담고 있습니다.

---

## Phase 1. 딜레이 라인 캘리브레이션 및 Monotonic LUT 생성

초기 측정된 로우 데이터(Raw Data)는 하드웨어 카운터의 한계로 인해 분절되고 노이즈가 섞여 있었습니다. 이를 해결하기 위해 5단계 수학적 보정 알고리즘을 적용했습니다.

### 1.1 Ghost Tap 필터링 및 병합
랩어라운드 경계에서 발생하는 메타스테빌리티 노이즈(갑자기 튀는 8번, 63번 탭 등)를 제거하기 위해 `numpy.diff` 기반의 고속 벡터 분할을 수행하고, 길이가 5 이하인 파편화된 조각을 전면 폐기하여 신뢰할 수 있는 구간(Main Segment)만 추출했습니다.

### 1.2 선형 외삽법 (Linear Extrapolation)
미측정된 외곽 영역(278~319번 탭)이 기본 보간법(Clamping)에 의해 동일한 값(4929ps)으로 평탄화되는 현상을 해결했습니다. 가장자리의 5개 탭의 물리적 기울기를 계산하여 직선을 연장하는 **선형 외삽법**을 적용하여 17.8ps 간격의 증가 추세를 완벽히 복원했습니다.

### 1.3 Monotonic (단조 증가) LUT 생성
기존 연산에서는 $5000\text{ps}$를 넘어가거나 음수로 떨어지는 값을 모듈러(`% 5000`) 처리하여 `4968`이나 `18`과 같은 랩어라운드가 발생했습니다. 하드웨어 뺄셈 로직의 단순화를 위해 **1) 기준점을 0으로 끌어올리고, 2) 모듈러 연산을 제거**하여 0부터 5000 이상까지 단 한 번의 꺾임 없이 **절대적 단조 증가(Monotonic Increasing)**를 이루는 `.coe` 파일을 최종 생성했습니다.

---

## Phase 2. 64-bit 절대 시간 변환 하드웨어 설계 (RTL)

생성된 LUT를 바탕으로, TDC Coarse 카운터와 Fine(LUT) 값을 결합하여 64비트 피코초(ps) 절대 시간을 산출하는 **5-Stage 파이프라인 로직**을 설계했습니다. 

### 2.1 하드웨어 물리적 공식의 적용
TDC의 딜레이 라인 특성상, Hit 신호가 클럭보다 일찍 도착할수록 더 먼 탭으로 전파됩니다. 따라서 절대 시간($T_{\text{abs}}$)은 다음과 같이 뺄셈으로 정의됩니다.
$$T_{\text{abs}} = (\text{Coarse} \times 5000) - \text{Fine\_LUT}$$

### 2.2 5-Stage Pipeline 최적화 설계의 핵심
*   **Stage 2 (DSP 곱셈 매핑):** 32비트 Coarse 카운터를 16비트 2개로 분할하여 $5000$을 곱함으로써 FPGA 내부 **DSP48E1 슬라이스에 1:1로 완벽히 매핑**시켜 라우팅 지연을 제거했습니다.
*   **Stage 2 (BRAM Latency 매칭):** Block Memory Generator의 **Primitives Output Register** 옵션을 활성화하여 2-Clock Latency를 확보하고, DSP 연산 사이클과 톱니바퀴처럼 맞물리도록 동기화했습니다.
*   **Stage 4~5 (Borrow-Lookahead Subtractor):** 64비트 뺄셈 시 발생하는 Carry-chain 병목을 피하기 위해, 하위 32비트를 먼저 빼서 **빌림(Borrow) 비트**만 추출한 뒤 다음 클럭에서 상위 32비트에 보상하는 고급 설계 기법을 적용했습니다.

---

## Phase 3. 하드웨어 검증 및 ILA 최적화 세팅

### 3.1 Debug Hub Clocking 에러 해결
MUX 클럭 사용으로 인한 `The debug hub core was not detected` 에러를 XDC 제약 조건(`set_property`)을 통해 외부 안정 클럭으로 강제 할당하여 해결했습니다.

### 3.2 Capture Control을 통한 유효 데이터 압축
250us 간격의 긴 대기 시간 동안 ILA 메모리가 쓰레기 데이터로 낭비되는 것을 막기 위해, **Capture Setup 조건에 `valid == 1`을 부여**하였습니다. 이를 통해 0부터 350 루프까지의 유의미한 위상 변화만 ILA RAM에 100% 밀도 있게 압축 캡처하는 데 성공했습니다.

---

## Phase 4. 소프트웨어 후처리 및 버그 디버깅 (The 200,000ps Bug)

64비트 절대 시간(수조 ps)에 파묻힌 17.8ps의 미세 위상 변화를 시각화하기 위해 파이썬 스크립트를 작성하는 과정에서 치명적인 수학적 모순을 극복했습니다.

### 4.1 역 모듈러 연산의 발견
Coarse 카운터로 인해 거대해진 숫자를 제거하기 위해 직선 피팅(`polyfit`)을 시도했으나 클럭 미세 오차(Clock Drift)로 인해 실패했습니다. 이를 하드웨어 공식의 역산으로 해결했습니다.
$$-T_{\text{abs}} = -(\text{Coarse} \times 5000) + \text{Fine\_LUT}$$
위 식에 $\pmod{5000}$을 적용하면 Coarse 항은 완전히 소멸하고 **순수한 Fine 위상값만 100% 복원**됩니다.

### 4.2 자가 증식 버그 (Feedback Loop Error) 해결
Unwrapping 로직 설계 시, 인접 값 비교(`diff`)를 보상된 값끼리 수행하는 오류를 범해 루프당 $200,000\text{ps}$씩 증가하는 대참사가 발생했습니다. 이를 **원본 배열(`raw_fine`) 끼리만 차분(diff)을 구하도록 수정**하여 무한 증식 피드백을 영구적으로 제거했습니다.

---

## 5. 최종 결론 및 성과

모든 소프트웨어 버그가 수정되고 데이터를 평균(Mean)낸 결과, **1번 루프부터 350번 루프까지 노이즈 하나 없이 완벽하게 우상향하는 계단 파형**을 확보했습니다.

*   측정된 루프당 위상 천이율: **$17.77 \text{ ps / loop}$**
*   **성과:** 하드웨어의 Wrap-around 한계를 소프트웨어로 극복하고, 고속 파이프라인 연산기를 성공적으로 설계하여, 며칠(Days) 단위의 긴 시간 속에서도 피코초(ps) 단위의 변화를 측정해 내는 **최고 스펙의 64-bit 절대 시간 측정 시스템 구축을 완료**하였습니다.
"""

# [TRIPLE_BACKTICKS] 환원 처리를 위한 코드 블록 텍스트 교체
# (이번 보고서에는 코드 블록보다는 설명 위주로 작성되어 그대로 출력해도 무방합니다.)
final_content = report_content

# 파일 저장
with open("TDC_Full_Project_Report.md", "w", encoding="utf-8") as file:
    file.write(final_content)

print("✅ 'TDC_Full_Project_Report.md' 파일이 성공적으로 생성되었습니다!")